server {
    listen 80;
    server_name matong.auberginewly.site;
    
    # 日志配置
    access_log /var/log/nginx/matong_access.log;
    error_log /var/log/nginx/matong_error.log;
    
    # 反向代理配置
    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 缓存配置
        proxy_buffering on;
        proxy_buffer_size 8k;
        proxy_buffers 8 8k;
    }
    
    # 静态文件缓存优化
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|css|js|woff|woff2|eot|ttf|otf)$ {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        
        # 缓存头
        expires 30d;
        add_header Cache-Control "public, immutable";
        
        # 日志可选关闭以减少IO
        access_log off;
    }
    
    # 安全头
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
    
    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml;
}

# HTTP重定向到HTTPS（可选，需要SSL证书时启用）
# server {
#     listen 80;
#     server_name matong.auberginewly.site;
#     return 301 https://$server_name$request_uri;
# }

# HTTPS配置（可选，需要SSL证书）
# server {
#     listen 443 ssl http2;
#     server_name matong.auberginewly.site;
#     
#     ssl_certificate /path/to/your/certificate.crt;
#     ssl_certificate_key /path/to/your/private.key;
#     
#     # 包含上面相同的反向代理配置...
# }
